<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Transaction Confirmation</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      padding: 80px 20px;
      min-height: 100vh;
    }

    .container {
      max-width: 700px;
      margin: 0 auto;
      background: white;
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
    }

    .header {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      padding: 50px 40px;
      text-align: center;
      position: relative;
    }

    .logo {
      width: 160px;
      height: 120px;
      background: white;
      border-radius: 24px;
      margin: 0 auto 24px;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
      overflow: visible;
    }

    .logo img {
      width: 100%;
      height: 100%;
      object-fit: contain;
      padding: 15px;
    }

    .logo-text {
      font-size: 44px;
      font-weight: 900;
      letter-spacing: 1px;
      color: #667eea;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .header h1 {
      color: white;
      font-size: 32px;
      font-weight: 700;
      margin-bottom: 8px;
      letter-spacing: -0.5px;
    }

    .header .subtitle {
      color: rgba(255, 255, 255, 0.85);
      font-size: 16px;
      font-weight: 400;
    }

    .status-badge {
      display: inline-block;
      background: rgba(76, 217, 100, 0.2);
      color: #4cd964;
      padding: 8px 20px;
      border-radius: 20px;
      font-size: 14px;
      font-weight: 600;
      margin-top: 20px;
      border: 2px solid rgba(76, 217, 100, 0.4);
    }

    .content {
      padding: 50px 40px;
      position: relative;
      overflow: hidden;
    }

    .transaction-card {
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      border-radius: 16px;
      padding: 30px;
      margin-bottom: 30px;
    }

    .amount {
      text-align: center;
      margin-bottom: 30px;
    }

    .amount-value {
      font-size: 48px;
      font-weight: 800;
      color: #2d3748;
      margin-bottom: 5px;
    }

    .amount-currency {
      font-size: 16px;
      color: #718096;
      font-weight: 500;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .transaction-details {
      display: grid;
      gap: 20px;
    }

    .detail-row {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 15px 0;
      border-bottom: 1px solid rgba(0, 0, 0, 0.08);
    }

    .detail-row:last-child {
      border-bottom: none;
    }

    .detail-label {
      font-size: 14px;
      color: #718096;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .detail-value {
      font-size: 16px;
      color: #2d3748;
      font-weight: 600;
      text-align: right;
      max-width: 60%;
    }

    .parties-section {
      display: flex;
      justify-content: space-between;
      align-items: center;
      gap: 20px;
      margin-bottom: 30px;
    }

    .party-card {
      background: white;
      border-radius: 12px;
      padding: 20px;
      text-align: center;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.05);
      flex: 1;
    }

    .party-label {
      font-size: 12px;
      color: #a0aec0;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: 10px;
    }

    .party-name {
      font-size: 18px;
      color: #2d3748;
      font-weight: 700;
      margin-bottom: 5px;
    }

    .arrow-icon {
      font-size: 28px;
      color: #667eea;
      font-weight: bold;
      flex-shrink: 0;
      margin: 0 10px;
    }

    .footer {
      background: #f7fafc;
      padding: 30px 40px;
      text-align: center;
      border-top: 1px solid #e2e8f0;
    }

    .footer-text {
      font-size: 13px;
      color: #718096;
      line-height: 1.6;
    }

    .footer-brand {
      margin-top: 15px;
      font-size: 14px;
      color: #a0aec0;
      font-weight: 600;
    }

    .footer-brand span {
      color: #667eea;
      font-weight: 800;
    }

    @media print {
      body {
        padding: 40px;
        background: white;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <div class="logo">
        <div class="logo-text">ZENITH</div>
      </div>
      <h1>Transfer Confirmed</h1>
      <p class="subtitle">Transaction processed successfully</p>
      <div class="status-badge">✓ Completed</div>
    </div>

    <!-- Content -->
    <div class="content">
      <!-- Amount -->
      <div class="amount">
        <div class="amount-value">{{amountMoney}}</div>
        <div class="amount-currency">{{currencyName}}</div>
      </div>

      <!-- Parties -->
      <div class="parties-section">
        <div class="party-card">
          <div class="party-label">From</div>
          <div class="party-name">{{senderName}}</div>
        </div>
        <div class="arrow-icon">→</div>
        <div class="party-card">
          <div class="party-label">To</div>
          <div class="party-name">{{recipientName}}</div>
        </div>
      </div>

      <!-- Transaction Details -->
      <div class="transaction-card">
        <div class="transaction-details">
          <div class="detail-row">
            <span class="detail-label">Date & Time</span>
            <span class="detail-value">{{date}}</span>
          </div>
          <div class="detail-row">
            <span class="detail-label">Status</span>
            <span class="detail-value">Confirmed</span>
          </div>
        </div>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p class="footer-text">
        This document was generated automatically by the Zenith Banking Application.<br>
        For any questions or concerns, please contact our support team.
      </p>
      <div class="footer-brand">
        Powered by <span>Zenith</span>
      </div>
    </div>
  </div>
</body>

</html>